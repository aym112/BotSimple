# Leave empty for local dev (Vite's proxy in vite.config.ts forwards /api, /auth to
# localhost:8001). Set to the deployed backend's URL (e.g. https://your-app.onrender.com)
# for production builds - see the root README's deployment section.
VITE_API_BASE_URL=
